A simple website which adheres to the brand guidelines. 
Designed with the help of a LLM. 
Uses HTML, CSS(color, consistency)and basic javascript(smooth animations).

**USAGE**
Clone the repo to a local file in windows OS (Linux, not tested) and run index.html
